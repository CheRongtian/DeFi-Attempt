events {}

http {
    upstream ethereum_rpc {
        server anvil:8545;
    }

    server {
        listen 8545;

        location / {
            proxy_pass http://ethereum_rpc;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
        }
    }
}
